# Compatibility Report Template Validation

Recorded: 2026-08-29

Command:

```bash
./scripts/validate-compatibility-reports.sh docs/compatibility/report-template.json
```

Result: pass. The command uses pinned `ajv-cli@5.0.0` and `ajv-formats@2.1.1` with the
repository Draft 2020-12 schema.
